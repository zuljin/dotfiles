#   ---------------------------------------
#   ALIASES - ubuntu
#   ---------------------------------------

# Folders
alias ..="cd .."
alias ...="cd ../.."
alias ....="cd ../../.."
alias ~="cd ~"
alias -- -="cd -"

# Easy file access
alias hosts="sudo vim /etc/hosts"
